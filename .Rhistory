install.packages('IRkernel')
Rkernel::installspec(user = FALSE)
IRkernel::installspec(user = FALSE)
IRkernel::installspec(user = FALSE)
IRkernel::installspec()
jupyter labextension install @techrah/text-shortcuts  # for RStudio’s shortcuts
install.packages("IRkernel")
